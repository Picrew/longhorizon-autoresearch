# Experiment log

Autonomous autoresearch loop over agentic long-horizon SFT (Qwen3-4B QLoRA, one
RTX 4090). Metric = held-out token-weighted val loss (lower is better), fixed
~22-min wall-clock training budget per experiment. This file is regenerated/
appended on `main`; per-experiment configs live on `exp/NNNN-slug` branches.

| # | slug | hypothesis | val loss | Δ vs prev best | kept | steps | train s |
|---|------|------------|----------|----------------|------|-------|---------|
| _pending first run_ | | | | | | | |

<!-- rows appended by the loop / steering check-ins -->
